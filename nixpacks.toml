[phases.setup]
nixPkgs = ['php82', 'php82Packages.composer', 'nodejs']

[phases.install]
cmds = ['composer install --no-dev --optimize-autoloader']

[start]
cmd = 'sh -c "php artisan migrate --force --seed 2>&1 && php artisan serve --host=0.0.0.0 --port=$PORT"'
